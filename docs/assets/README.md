# CivicPulse Visual Evidence

These images are committed so the GitHub README shows the working project instead of placeholder diagrams.

- `dashboard-overview.png`: captured from the live React dashboard connected to the local FastAPI service.
- `mitigation-and-benchmark.png`: captured from the live mitigation and model-comparison sections.
- `api-docs.png`: captured from the live FastAPI OpenAPI documentation at `/docs`.
- `demo-video-thumbnail.png`: extracted from `demo/civicpulse_demo.mp4`.
